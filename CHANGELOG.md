# CHANGELOG

프로젝트의 중요한 기획, 구현, 밸런스, 배포 변경을 기록합니다. 단순한 오탈자 수정은 생략할 수 있지만, 게임 규칙이나 플레이 경험이 바뀌면 반드시 기록합니다.

## 2026-08-08 (배포)

### Added

- GitHub Pages로 배포: **https://llaudeer.github.io/ai-ceo-visual-novel/** — 로그인·설치·승인 없이 즉시 플레이
- 공개 저장소 `llaudeer/ai-ceo-visual-novel` 생성, `index.html` 단일 파일 배포

### Fixed

- 이전에는 claude.ai Artifact 링크로 공유했는데, 이 방식은 뷰어가 claude.ai에 로그인돼 있어야만 열려 "별도 승인 없이 접근 가능"이라는 대회 제출 요구사항을 충족하지 못했다. GitHub Pages 정적 배포로 교체해 해결

### Notes

- 앞으로 게임을 수정할 때는 로컬 `index.html`을 고친 뒤, 같은 내용을 `ai-ceo-visual-novel` 저장소의 `main` 브랜치에 다시 푸시하면 위 링크가 그대로 갱신된다

## 2026-08-08

### Changed

- 텍스트 대시보드 형식을 **비주얼 노벨 형식**으로 전면 재구성. 캐릭터 스탠딩 연출, 대사창, 타이핑 출력, 장 표지, 배경 전환, 효과음 추가
- 결과 패널을 없애고, 선택 결과를 캐릭터 대사와 나레이션으로 재생하도록 변경. 상태 증감은 HUD 위에 팝업으로 표시
- HUD 지표 순서를 중요도 순으로 재배치 (`자금 → AI 의존 → 신뢰 → 사기 → …`). 좁은 화면에서 뒤쪽부터 숨으므로, 핵심 지표인 AI 의존도가 먼저 잘리지 않게 함
- 데모 결정 수를 11개 → **12개**로 확대 (`인턴의 보고서` 추가)

### Added

- 캐릭터 4명 추가: 정유나(QA 인턴), 한도윤(퍼블리셔 이사), 오세라(기자), 강태석(ARIA 운영사 전략 총괄). 총 8명
- 전 캐릭터 스프라이트를 인라인 SVG로 직접 구현. 머리·피부·의상·소품을 파라미터로 조합하고, 표정 8종(눈/눈썹/입 조합)을 장면별로 지정
- ARIA는 사람이 아닌 회전하는 링과 코어로 표현하고, 대사만 고정폭 서체로 출력해 사람과 목소리를 구분
- **조명 시스템**: `aiDependence`에 따라 무대 조명이 호박색(사람)에서 보라색(알고리즘)으로 실시간 보간. 상태를 숫자가 아니라 화면 색으로 읽게 함
- 배경 7종(주간/야간 사무실, 회의실, 서버실, 기자회견장, ARIA 공간, 새벽)을 인라인 SVG로 구현
- WebAudio 기반 효과음(타이핑, 선택, 증감, 경보, 장 전환, 엔딩)과 음소거 토글. 오디오 파일 없음
- `resolveEffects()` 추가 — 플래그에 따라 선택 효과가 달라지는 조건부 보정. 유나의 QA 보고서를 수용하면(`heededYuna`) 이후 결제 사고의 전수 환불 비용이 320만 → 180만 원으로 감소

### Fixed

- 장 표지가 떠 있는 동안 이전 장면의 입력 모드(`choices`)가 살아 있어, 표지에서 숫자 키를 누르면 다음 장면의 선택지가 즉시 선택되던 문제. `card` 모드를 추가해 표지 위 입력을 차단
- 창밖 도시 불빛을 퍼센트 좌표로 그려 뷰포트마다 위치와 크기가 달라지던 문제. viewBox 단위로 변경
- 책상 램프가 중앙 캐릭터의 머리 위에 겹쳐 보이던 배치
- 눈썹이 앞머리에 가려 표정이 읽히지 않던 문제
- 나레이션 중 화자가 없어 캐릭터 전원이 과도하게 어두워지던 문제
- 좁은 화면에서 캐릭터 3명이 화면 밖으로 잘리던 문제. 620px 이하에서는 말하는 사람만 중앙에 표시
- 음수 `gap`, 깨진 색상 문자열 등 렌더에 반영되지 않던 잘못된 CSS/SVG 값

### Notes

- 밸런스 재검증: 전체 분기 **218,790경로** 시뮬레이션. 막히는 경로 0, 파산 1,650경로(0.75%), 엔딩 6종 모두 도달 가능
- 데스크톱·모바일(375px) 양쪽에서 12장면 완주 검증. 콘솔 오류 0, 가로 스크롤 없음
- 여전히 외부 요청 없음: 폰트·이미지·오디오 파일·API 호출 모두 사용하지 않음
- 남은 작업: 배포 링크 공개 전환, 3분 플레이 영상, 영어 지원 범위 결정

## 2026-08-07

### Added

- 플레이 가능한 대회용 데모 구현 (`index.html` 단일 파일, 빌드 도구 없음)
- `GameState` 모델과 순수 상태 전이 함수 `applyEffects(state, effects)` 구현
- 7개 상태값 HUD와 선택 직후 증감 표시
- 고정 이벤트 11개와 흐름: 창업 → 첫 프로젝트 → ARIA 등장 → 성장 압박 → AI 도입 분기 → 조직 갈등 → 출시 → 버그 → 여론 → 정체 공개 → 최종 기준 선택
- 직원 3명(박지훈/이서연/최민)과 AI 비서 ARIA의 장면별 조언 카드
- 버그 이벤트의 3종 대응(전수 환불 / 조용한 처리 / 기능화)
- 엔딩 6종: 청산, 통합(인수), 통제된 자동화, 사람의 속도, 정체, 미결
- 선택 결과 패널(변화량 칩 + 팀 반응), 엔딩 요약 화면(최종 상태 + 11개 결정 로그)
- 숫자 키 1~4 선택, Enter 진행, 포커스 링, `처음부터` 버튼
- 렌더 실패 시 복구 가능한 오류 화면

### Changed

- 기술 스택 확정: 프레임워크 없는 단일 HTML + 바닐라 JS. 심사 링크에서 설치·로그인·네트워크 없이 즉시 실행되는 것을 최우선으로 함
- `README.md` 빠른 실행 안내를 실제 실행 방법으로 갱신
- `ROADMAP.md` Phase 0~2 완료 처리, `TODO.md` P0 전체와 P1 대부분 완료 처리

### Notes

- 밸런스 검증: 전체 분기 73,032경로를 시뮬레이션. 파산 종료 837경로(약 1.1%)로, 사치스러운 선택이 연속될 때만 발생. 엔딩 6종 모두 도달 가능
- 운영비는 씬별 `burn` 값으로 명시하고 매출의 80%가 상쇄하는 단순 모델. 플레이어가 원인을 추론할 수 있도록 랜덤 요소 없음
- 데모 전체가 결정론적이며 생성형 AI API를 호출하지 않음 (BIBLE 11장)
- 남은 작업: 사운드, 배포 링크, 3분 플레이 영상, 영어 지원 범위 결정

## 2026-08-06

### Added

- 프로젝트 전체 기획 문서 세트 생성
- 대회용 데모와 정식 출시 버전의 범위 분리
- AI 비서, 인간 직원, 버그 대응, UI, 코딩 규칙 문서화

### Current status

- 문서 기준의 프로젝트 뼈대 단계
- 실제 게임 코드와 배포 링크는 아직 연결되지 않음
- 다음 우선 작업은 `TODO.md`의 P0 항목

## 기록 형식

변경 시 다음 형식을 유지합니다.

```md
## YYYY-MM-DD

### Added
- 새로 추가한 기능이나 콘텐츠

### Changed
- 기존 규칙이나 화면 변경

### Fixed
- 버그와 회귀 수정

### Notes
- 밸런스 수치, 결정 이유, 알려진 문제
```
